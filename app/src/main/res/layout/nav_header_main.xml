<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/header_image_view"
        android:layout_width="@dimen/nav_header_profile_image_dimension"
        android:layout_height="@dimen/nav_header_profile_image_dimension"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:background="@drawable/circle_image_filled"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/header_user_name"/>

    <TextView
        android:id="@+id/header_initial_letter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/header_profile_letter_size"
        app:layout_constraintBottom_toBottomOf="@id/header_image_view"
        app:layout_constraintEnd_toEndOf="@id/header_image_view"
        app:layout_constraintStart_toStartOf="@id/header_image_view"
        app:layout_constraintTop_toTopOf="@+id/header_image_view"
        tools:text="A"/>

    <TextView
        android:id="@+id/header_user_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/header_image_view"
        tools:text="Android Studio"/>

    <TextView
        android:id="@+id/header_user_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/header_user_name"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="android.studio@android.com"/>

</android.support.constraint.ConstraintLayout>
